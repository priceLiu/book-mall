# 首页「发现更多」视频

将 16:9 的 `.mp4` 放在此目录；服务端会扫描并生成列表（仅元数据，不预读文件内容）。

## 性能说明

- 首页 **不会** 在打开时批量下载所有 mp4
- 首批展示 12 条，其余点「加载更多」
- 卡片进入视口后才挂载 `<video>`，**悬停** 才开始拉流
- 弹窗播放时才完整加载所选视频

## 上线建议

生产环境建议把 `playbackSrc` 改为 CDN / OSS 地址（见 `lib/landing-showcase.ts` 与后续 API），本地 `public/video` 仅用于开发占位。
