# next build / next start（NODE_ENV=production）时加载。勿写密钥；密钥仍由云托管或 .env.production.local 注入。
# Docker 运行时若未设置同名变量，docker-entrypoint 也会回落到这些域名。
NEXTAUTH_URL=https://book.ai-code8.com
TOOLS_PUBLIC_ORIGIN=https://tool.ai-code8.com
