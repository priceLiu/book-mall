🎬模拟完整交互演示（方便你理解产品行为）
用户上传 4 张图片，输入指令：
@图片1@图片2@图片3@图片4帮我用这些素材生成3个种草视频脚本（并带有口播文案），视频时长预计20s，给我选择确认
👉 AI 输出 Step2 结果：**素材解析 Markdown** + 三套脚本 Markdown + 末尾 **` ```seed-video ` JSON 围栏**（系统只解析 JSON；界面隐藏围栏）+ 单选卡片，停在这里，等待用户选脚本。
用户回复：选脚本二
👉 AI 输出 Step3，两个制作模式选项，停在这里，等待选择。
用户回复：选方案②：按精细成片流程制作
👉 AI 输出 Step4，A/B 风格选项，停在这里，等待选择。
用户回复：选A方案：甜美种草风
👉 AI 输出 Step5，完整逐镜头表格（时间切片、参考图、镜头描述、视频 Prompt、口播）。
此时下游工具拿到表格数据：
1. 循环每一行，拿【AI 视频生成提示词】调用 Pika / 可灵，把静态图生成动态片段；
2. 拿【口播文案】+ 音色标签调用 TTS 生成配音；
3. 按时间切片拼接视频片段、音频、字幕，输出完整 20s 成片。