# 见 README.md；以下为本地对接 book-mall 的推荐变量

# book-mall 根地址（浏览器直连 book-mall 拉账单时需要；走开发代理时服务端也可用 BOOK_MALL_URL）
NEXT_PUBLIC_BOOK_MALL_URL=http://localhost:3000

# ✅ 推荐：浏览器直连 book-mall，使用你在主站的真实 NextAuth 登录态拉取账单。
#     localhost 跨端口（3002↔3000）是同站点，SameSite=Lax 的 session Cookie 会被发送。
#     必要的 CORS 已在 book-mall 端按 FINANCE_WEB_ORIGINS 放行，credentials 已通过。

# ❌ 不要默认开下面这两行——一旦开，账单将以 FINANCE_DEV_USER_ID 指代的固定用户身份拉取，
#     无视你的真实登录态（你登录的是 A，但看到 B 的明细）。仅在故意「模拟某个用户」时使用。
# 服务端代请求 book-mall（脚本/调试用）
# BOOK_MALL_URL=http://localhost:3000
# 本地账单页走 /api/dev/book-mall-account-billing 而不是浏览器直连
# NEXT_PUBLIC_FINANCE_USE_DEV_PROXY=1
# 代理路由使用的 User.id（与 book-mall 库中用户一致；可用 cd book-mall && pnpm billing:print-dev-user）
# FINANCE_DEV_USER_ID=

# 仅在「不走代理 + 显式 ?asDev=1」时生效；book-mall 端须 FINANCE_ALLOW_DEV_USER_QUERY=1
# NEXT_PUBLIC_FINANCE_DEV_USER_ID=
