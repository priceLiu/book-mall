# 见 README.md
# 云托管生产：见 deploy/tencent/finance-web.env.example

# ── 本地开发（可选）────────────────────────────────────────────
# pnpm dev:all 后无需本文件亦可联调：
#   - 未配置时 development 默认主站 http://localhost:3000
#   - 浏览器请求统一走 finance-web 同源 /api/book-mall/*（BFF 转发 Cookie）
#   - book-mall 不必单独配 FINANCE_WEB_ORIGINS
#
# 若需覆盖默认主站地址，可取消注释：
# NEXT_PUBLIC_BOOK_MALL_URL=http://localhost:3000
# BOOK_MALL_URL=http://localhost:3000

# ── 生产（必填，见 deploy/tencent/finance-web.env.example）──────
# NEXT_PUBLIC_BOOK_MALL_URL=https://book.ai-code8.com
# BOOK_MALL_URL=https://book.ai-code8.com

# ❌ 不要默认开 dev 模拟用户（登录 A 却看到 B 的明细）：
# NEXT_PUBLIC_FINANCE_USE_DEV_PROXY=1
# FINANCE_DEV_USER_ID=
# NEXT_PUBLIC_FINANCE_DEV_USER_ID=
