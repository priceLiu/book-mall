# 见 README.md；以下为本地对接 book-mall 的推荐变量

# book-mall 根地址（浏览器直连管理端明细时需要；走开发代理时服务端也可用 BOOK_MALL_URL）
NEXT_PUBLIC_BOOK_MALL_URL=http://localhost:3000

# 服务端代请求 book-mall（与 NEXT_PUBLIC_FINANCE_USE_DEV_PROXY=1 配合时可二选一或同填）
# BOOK_MALL_URL=http://localhost:3000

# 本地账单页走 /api/dev/book-mall-account-billing，避免跨域 Cookie 问题（生产勿开）
# NEXT_PUBLIC_FINANCE_USE_DEV_PROXY=1

# 代理路由使用的 User.id（须与 book-mall 库中用户一致；可用 cd book-mall && pnpm billing:print-dev-user）
# FINANCE_DEV_USER_ID=

# 仅开发不走代理时：在 book-mall 设 FINANCE_ALLOW_DEV_USER_QUERY=1 时，可填某个 User.id，请求将带 ?devUserId=
# NEXT_PUBLIC_FINANCE_DEV_USER_ID=
