# next dev 自动加载（优先级低于 .env*.local）
NEXT_PUBLIC_BOOK_MALL_URL=http://localhost:3000

# 本地 :3002→:3000 跨域带不上 Cookie 时，账单页走 finance-web 服务端代理（须配合 .env.local 的 FINANCE_DEV_USER_ID）
NEXT_PUBLIC_FINANCE_USE_DEV_PROXY=1

# 代理路由读取（勿提交真实 id 到仓库；在 .env.local 覆盖）
# FINANCE_DEV_USER_ID=
