# next dev 自动加载（优先级低于 .env*.local）
NEXT_PUBLIC_BOOK_MALL_URL=http://localhost:3000

# ⚠️ 默认浏览器直连 book-mall。localhost 跨端口（3002↔3000）属同站点，
# NextAuth 默认 SameSite=Lax 的 session Cookie 会被发送，**不要**默认开代理：
# 一旦开代理，账单页将永远以 FINANCE_DEV_USER_ID 指代的固定用户拉取数据，
# 无视你浏览器登录的真实账号——会出现「登录 A、看到 B 的明细」。
# 仅在「故意以某个用户 id 模拟」时再单独在 .env.local 里打开下面这两行：
# NEXT_PUBLIC_FINANCE_USE_DEV_PROXY=1
# FINANCE_DEV_USER_ID=
