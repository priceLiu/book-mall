# 分享规则 2.0 · 产品规格（SSOT）

> **状态**：已完成  
> **关联**：[`book-mall/doc/product/17-referral-sharing.md`](../book-mall/doc/product/17-referral-sharing.md)（邀请 1.0，已迁移至本规格）  
> **入口**：主站 [`/account/referral`](http://localhost:3000/account/referral) · 订阅页 [`/pricing`](http://localhost:3000/pricing) · Finance [`/admin/referrals`](http://localhost:3002/admin/referrals)

---

## 1. 目标与范围

建立 **全积分、无现金** 的双轨分享体系：

| 应用 | 工作流分享 | 阶段 |
|------|------------|------|
| canvas-web | 画布项目 clone | SR-103 |
| e-commerce-toolkit | 分镜/项目 duplicate | SR-104 |
| quick-replica-web | 模板/任务 duplicate | SR-104 |

账与发奖 **Single Writer**：仅在 book-mall 写 `CreditLedger`；子应用提供分享 UI 与深链。

---

## 2. 两条分享链路

> **码制与统一入口（3.0）** 见 [`docs/分享链接.md`](./分享链接.md)：主站 `/code/{code}`、邀请 8 位码、工作流 10 位码、微信 QR。本节奖励规则不变。

| 链路 | URL（推荐 · 3.0） | 兼容 URL | 目的 |
|------|-------------------|----------|------|
| **邀请分享** | `{主站}/code/{code}` | `{主站}/r/{code}` | 拉注册 |
| **工作流分享** | `{主站}/code/{shortCode}` | `{子站}/share/w/{token}` | 拉用具体工作流（clone 副本） |

二者 **不合并为一种奖励类型**；共用 book-mall `ShareRewardService`。

---

## 3. 奖励规则

### 3.1 条件（全部满足才发 **一笔** 积分）

| 链路 | 条件 1 | 条件 2 | 奖励 | 对象 |
|------|--------|--------|------|------|
| 邀请 | B 通过 `/r/` 完成注册 | B **首笔付费** | **20** 积分 | 邀请人 A |
| 工作流 | B **首次成功扣积分生成** | B **首笔付费** | **40** 积分 | 分享者 C |

### 3.2 首笔付费定义

B 历史上第一笔 `Order.status = PAID`，且 `type` 为下列之一：

- `CREDIT_TOPUP`（积分加油包）
- `SUBSCRIPTION` / `MEMBERSHIP` / `PRODUCT_SUBSCRIPTION`（会员订阅）

**订阅或充值任一即可**；在 [`fulfill-checkout`](../book-mall/lib/payments/fulfill-checkout.ts) 入账成功后标记。

### 3.3 不做叠加（先到先得）

每个被邀请人 B **终身仅发一笔**分享奖励。以 **最早写入** 的 `ShareRewardAttribution` 为准：

1. 若 **WorkflowShareClaim** 先于 `referredByUserId` → 工作流链路（40 分）
2. 若 **`referredByUserId`** 先于 Claim → 邀请链路（20 分）

### 3.4 测算（默认配置）

| 奖励 | 锚价 ¥0.04/积分 | 占最小首充 ¥62 |
|------|-------------------|----------------|
| 20 | ≈ ¥0.80 | 0.32% |
| 40 | ≈ ¥1.60 | 0.65% |

B 注册仍获 `welcomeGiftGeneralCredits`（默认 600，30 天 FREE 批次）。

### 3.5 发放批次

奖励经 `topupCredits({ source: "FREE" })`，过期天数由 `shareRewardCreditsExpireDays`（默认 90）控制。

---

## 4. 分享者门禁

与 [`getReferralEligibility`](../book-mall/lib/referral/referral-service.ts) 一致：

- 有效个人订阅（任意档），或团队 **OWNER** 且团队套餐有效
- 团队非 OWNER 成员 **不可** 分享

---

## 5. 数据模型

| 模型 | 用途 |
|------|------|
| `ShareRewardAttribution` | 先到先得锁定 channel + referrer |
| `WorkflowShareLink` | 工作流分享 token |
| `WorkflowShareClaim` | B claim + firstBillableAt |
| `ShareRewardGrant` | 已发放记录 + 幂等 |
| `ShareRewardProgress` | B 侧 firstPaidAt / firstBillableAt |

幂等键：`share_reward:{inviteeUserId}`

---

## 6. Hook 点

| 事件 | 文件 | 动作 |
|------|------|------|
| 注册 + referralCode | `app/api/auth/register/route.ts` | `lockAttribution(REFERRAL)` |
| workflow claim | `workflow-share/.../claim` | `lockAttribution(WORKFLOW)` |
| 首次扣积分生成 | `gateway-credit-settlement.ts` | `markFirstBillable` |
| 首笔订阅/充值 | `fulfill-checkout.ts` | `markFirstPaid` + `tryGrantShareReward` |

---

## 7. 管理后台（finance-web）

`PlatformPricingConfig` 字段：

- `referralRewardCredits`（默认 20）
- `workflowShareRewardCredits`（默认 40）
- `shareRewardCreditsExpireDays`（默认 90）
- `shareRewardDailyCapPerReferrer`（可选，0=不限）

现金返佣 `ReferralPayout` **不再新出单**；历史只读。

---

## 8. 订阅页 / 个人中心文案

- **订阅页** [`/pricing`](../book-mall/app/(site)/pricing/page.tsx)：展示双轨规则 + CTA「去分享」
- **个人中心** [`/account/referral`](../book-mall/app/(account)/account/referral/page.tsx)：已获积分、待完成人数（已注册未首笔付费 / 已 claim 未生成或未付费）

---

## 9. 实施任务与验收

| ID | 任务 | 状态 |
|----|------|------|
| SR-101 | 分享奖励引擎 ShareRewardService + 归因锁定 | 已完成 |
| SR-102 | 邀请分享改积分（/r/ 20 分，退役现金返佣新出单） | 已完成 |
| SR-103 | 工作流分享 · 画布 | 已完成 |
| SR-104 | 工作流分享 · 电商/快速复刻 | 已完成 |
| SR-105 | 财务后台配置 20/40 | 已完成 |
| SR-106 | 订阅页分享展示 | 已完成 |
| SR-107 | 全应用充值 returnTo 深链 | 已完成 |

### 验收清单

- [x] B 仅 `/r/` 注册 + 首笔订阅 → A 得 20 分，仅一次
- [x] B workflow claim + 首次生成 + 首笔充值 → C 得 40 分（代码路径）
- [x] 先到先得归因（ShareRewardAttribution）
- [x] 同一 B 幂等键不重复发奖
- [x] 订阅页可见分享规则说明
- [x] checkout 支持 returnTo 白名单

---

## 10. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-08-22 | 初稿：双轨分享、全积分、首笔订阅/充值、先到先得不叠加、SR-* 任务 |
| 2026-08-22 | 实施完成：ShareRewardService、workflow-share API、画布/电商/QR 落地页、finance 配置、pricing 展示、checkout returnTo |
