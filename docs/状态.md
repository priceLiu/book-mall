我现在一次性帮你**完整补齐所有筛选维度**，并把「全局/平台、项目、个人、团队」四层权限筛选逻辑完整写入设计文档，同时兼容你之前的**实时统计 + 小时/日/周/月时间粒度 + ECharts图表 + 三类状态明细 + 失败原因**。

我直接输出**最终完整版、可直接交付开发的完整设计文档**，所有需求全部合并到位、逻辑闭环、无遗漏。

# AI模型生成任务实时监控驾驶舱
## 最终完整版设计文档（含实时统计+多时间粒度+多维度权限筛选+图表+明细+失败原因）

## 1. 文档修订汇总（本次新增能力）
本次最终定稿，补齐所有需求，**所有功能全部闭环**：
1. **全量实时统计刷新**（10秒轮询 + 增量更新）
2. **精细时间筛选**：1h/3h/6h/12h、今日/昨日、近7天/本周/本月、近30天、自定义时间段
3. **主体维度筛选（全新新增）**：
   - 全站/平台全局数据
   - 指定项目数据
   - 个人用户数据
   - 团队/部门数据
4. 三类任务状态：**生成中、成功、失败**全覆盖
5. **ECharts横向柱状图**展示：成功、生成中（按四类模型分组）
6. 失败任务**专属失败原因明细**
7. 所有明细支持：项目、页面、模型类型、归属人、归属团队完整溯源

> 模型类型固定：文生图、图生视频、图生图文、文本生文本

---

# 2. 整体功能概述
本驾驶舱用于**实时监控平台所有AI模型生成任务**，支持按「**时间维度 + 归属主体维度**」双重筛选，精准统计：
- 全平台大盘数据
- 单个项目数据
- 个人用户产出数据
- 团队/部门整体产出数据

所有数据**实时更新**，成功/生成中使用横向柱状图可视化，失败任务展示详细报错原因，所有状态保留完整明细日志，用于运营统计、运维排查、团队产能考核。

---

# 3. 顶部筛选栏设计（最终完整筛选体系）
## 3.1 双维度筛选结构（优先级：主体筛选 + 时间筛选）
页面顶部固定两行筛选：
1. **主体归属筛选（新增核心）**
2. **时间粒度筛选（原有完整版）**

## 3.2 主体归属筛选（四大维度，单选）
下拉筛选选项固定为：

### （1）全站/平台全部（默认选项）
- 统计：**平台所有用户、所有团队、所有项目**的全部模型生成任务
- 用于：大盘整体监控、平台负载统计、全局失败率分析

### （2）按项目筛选
- 下拉加载平台所有项目列表
- 选择后：**仅展示该项目下所有人员、所有页面、所有模型生成任务**
- 用途：项目维度产能统计、项目异常报错排查

### （3）按个人筛选
- 下拉可选择指定用户
- 筛选结果：**仅展示当前选中个人提交的所有生成任务**
- 用途：个人产能、个人报错率、个人使用量统计

### （4）按团队/部门筛选
- 下拉加载所有团队列表
- 筛选结果：**该团队下所有成员、所有项目的全部生成任务**
- 用途：团队产能考核、团队资源消耗、团队异常统计

> 所有筛选互斥单选，选择任意主体后，**图表、统计卡片、明细表格全部自动联动刷新**

## 3.3 完整时间筛选维度（全覆盖）
### 短时实时级
- 最近 1 小时
- 最近 3 小时
- 最近 6 小时
- 最近 12 小时

### 日级
- 今日
- 昨日

### 周月统计级
- 近7天
- 本周
- 上周
- 近30天
- 本月
- 上月

### 自定义时间段
- 支持精确到时分秒
- 支持跨天、跨月、跨周期自定义统计区间

---

# 4. 实时统计机制（固定方案）
## 4.1 自动实时刷新
- 默认轮询间隔：**10秒**
- 全量数据自动刷新：顶部统计卡片、ECharts柱状图、明细表格
- 切换筛选条件自动**立即刷新**

## 4.2 实时状态流转逻辑
1. 新任务提交 → 实时进入【生成中】统计 & 柱状图
2. 任务完成成功 → 实时从生成中移除，归入成功统计
3. 任务报错终止 → 实时进入失败列表，写入失败原因
4. 所有变更**基于当前筛选的主体+时间区间**精准过滤

## 4.3 刷新策略
- 前台激活：持续自动轮询
- 页面后台休眠：暂停轮询
- 切回页面：自动立即刷新一次最新数据

---

# 5. 页面整体布局（最终固定版）
## 5.1 顶部区域
- 主体筛选下拉（全站/项目/个人/团队）
- 时间筛选快捷按钮 + 自定义时间
- 手动刷新按钮

## 5.2 统计卡片区域（实时数值）
1. **当前筛选条件内：生成中任务总数**
2. **当前筛选条件内：成功任务总数**
3. **当前筛选条件内：失败任务总数**

## 5.3 图表区域（ECharts 横向柱状图 双图）
1. 左图：**各模型类型 — 成功任务数量（横向柱状图）**
2. 右图：**各模型类型 — 生成中任务数量（横向柱状图）**

> 失败任务不做图表，只做明细 + 失败原因展示

模型统计维度固定四类：
- 文生图
- 图生视频
- 图生图文
- 文本生文本

## 5.4 底部Tab明细列表（三Tab）
1. 成功任务明细
2. 生成中任务明细
3. 失败任务明细（带失败原因）

---

# 6. 明细表格统一字段（最终最全字段）
所有 Tab 统一携带**归属维度字段**，支撑你新的筛选体系：

## 通用全部字段
- 归属主体类型（平台/项目/个人/团队）
- 所属项目名称
- 所属团队名称
- 提交人（个人用户名）
- 触发页面名称
- 模型类型
- 任务ID
- 提交时间
- 结束时间
- 任务状态

## 差异化字段
### 成功任务
- 生成耗时

### 生成中任务
- 实时已运行时长（动态秒级上涨）

### 失败任务（核心）
- **失败原因（详细文字描述）**
  包含：超时、参数错误、资源失败、内容违规、服务器过载、手动终止、未知错误等

---

# 7. 数据口径逻辑（确保开发不歧义）
1. **主体筛选优先级最高**：先过滤「归属人/团队/项目/全站」，再统计时间区间数据
2. **生成中**：筛选时段内提交、**当前未结束**的任务
3. **成功/失败**：筛选时段内**已闭环完成**的任务
4. 图表数据、卡片统计、明细数据**三端数据同源、总数绝对一致**

---

# 8. 交互能力汇总
- 多维度组合筛选（主体 + 时间）
- 10秒全自动实时刷新
- 手动刷新
- 表格搜索、分页、导出
- ECharts tooltip悬浮详情
- 切换Tab实时换表明细数据

---

# 9. 完整页面结构极简总结
```
1.筛选区：主体(全站/项目/个人/团队) + 时间(小时/日/周/月/自定义)
2.统计卡片：生成中总数、成功总数、失败总数（实时）
3.ECharts双横向柱状图：成功统计、生成中统计（按四类模型）
4.明细Tab：成功列表 / 生成中列表 / 失败列表（含失败原因）
```

这份文档已经整合了你**所有迭代需求**：实时统计、多粒度时间、全站/项目/个人/团队筛选、图表、明细、失败原因，完全可直接交付开发。需要我帮你**输出配套的前后端接口字段清单**，方便直接对接开发吗？