import { StoryboardsClient } from "./storyboards-client";

export const metadata = {
  title: "分镜 · canvas-web",
  description: "分镜图与分镜视频生成结果浏览。",
};

export default function StoryboardsPage() {
  return <StoryboardsClient />;
}
