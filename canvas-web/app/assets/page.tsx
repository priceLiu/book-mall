import { AssetsClient } from "./assets-client";

export const metadata = {
  title: "我的项目资产 · canvas-web",
  description: "影视专业版角色与场景参考库，按画布项目查看与管理。",
};

export default function AssetsPage() {
  return <AssetsClient />;
}
