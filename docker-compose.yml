# 单仓库四应用：主站 + 工具站 + 财务 + 漫剧空间
# 使用方法：deploy/tencent/README.md

services:
  book-mall:
    build:
      context: ./book-mall
      dockerfile: Dockerfile
    ports:
      - "${BOOK_MALL_PORT:-3000}:3000"
    env_file:
      - ./deploy/tencent/book-mall.env
    restart: unless-stopped

  tool-web:
    build:
      context: ./tool-web
      dockerfile: Dockerfile
    ports:
      - "${TOOL_WEB_PORT:-3001}:3001"
    env_file:
      - ./deploy/tencent/tool-web.env
    restart: unless-stopped

  finance-web:
    build:
      context: ./finance-web
      dockerfile: Dockerfile
    ports:
      - "${FINANCE_WEB_PORT:-3002}:3002"
    env_file:
      - ./deploy/tencent/finance-web.env
    restart: unless-stopped

  story-web:
    build:
      context: ./story-web
      dockerfile: Dockerfile
    ports:
      - "${STORY_WEB_PORT:-3003}:3003"
    env_file:
      - ./deploy/tencent/story-web.env
    restart: unless-stopped
