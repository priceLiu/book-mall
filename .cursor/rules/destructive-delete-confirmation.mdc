---
description: 用户触发的删除须二次确认；涉及 OSS 时第二次须明确不可恢复与云存储
alwaysApply: true
---

# 删除与二次确认（含 OSS）

## 适用范围

凡产品界面中用户主动发起的 **删除**（删除库条目、取消收藏、清理资源等），须 **连续两次确认** 后才调用删除接口。

## 交互要求

1. **第一次**：说明删除对象（例如「从我的视频库删除本条」）。
2. **第二次**：明确 **不可恢复**；若接口会删除或尝试删除 **OSS / 云端文件**，第二次文案须写明 **云端存储（OSS）**。
3. 实现上使用两次 `window.confirm`、自定义 Modal 二次确认、或等效「必须点两次肯定」的流程均可，禁止单次确认即发起破坏性删除 API。

## 服务端

API 层不替代人工确认；**不在** API 内用 `confirm`。需在 **调用方 UI** 完成二次确认。

## 涉及 OSS 删除的现有入口（须遵守）

- 工具站：`我的视频库`、`我的图片库`、`我的衣柜`、管理后台「工具资源库」等删除会触发主站 `deleteManagedOssObjectByUrl` 的路径（衣柜当前仅删库记录，仍须二次确认；第二次可用通用不可恢复文案）。

新增类似功能时延续同一模式。
