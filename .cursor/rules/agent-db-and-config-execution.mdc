---
description: Agent 可执行数据库与配置类的增量操作（删除除外）；执行前须先说明计划
alwaysApply: true
---

# 数据库与配置：Agent 执行边界

## 可由 Agent 直接执行（并应尽量代为执行）

在 **不涉及删除** 的前提下，以下事项可由 Agent 使用终端/编辑工具完成；**动手前必须先向用户简短说明将要做什么及范围**，再执行。

- **数据库**：新增/修改 Prisma schema、编写迁移 SQL、`prisma migrate deploy` / `migrate dev`、`prisma generate`、`prisma migrate status`、按文档套用本地 `.env.local` 的数据库命令（如 `pnpm db:deploy`、`pnpm db:migrate`）。
- **配置文件**：新增或编辑示例/模板 env（如 `.env.example`）、部署与环境文档中与连接相关的占位说明、`docker-compose` / CI 中非密钥占位、`next.config` 等与运行相关的安全配置项。
- **说明**：执行可能影响远端或多环境的命令前，在一句预告里写清目录与工作目录（例如 `book-mall`）。

## 删除类：不由 Agent 擅自执行

以下须 **用户明确要求** 后才可操作；默认 **不主动执行**：

- 删除数据库表、清空业务数据、`migrate reset`、Drop 库等破坏性命令。
- 删除 `.env` / `.env.local` 等含密钥的文件或整段密钥内容。
- 删除迁移历史目录、强制改写已发布迁移等易导致团队不一致的操作。

若用户仅说「清理」，应先确认是否包含删除数据或密钥，再动手。

## 执行前预告（必填）

每次将要运行 **数据库迁移、schema 变更、或写入/修改配置文件** 时，先用一两句话说明：**改什么文件 / 跑什么命令 / 预期结果**，再执行；执行结束后用简短结论收尾（成功或报错要点）。
