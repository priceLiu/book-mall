---
description: Gateway 日志 UI（Tip/Status）与失败字段 failCode/failMessage 规范
globs: gateway-web/components/logs/**,gateway-web/app/globals.css,gateway-web/docs/design.md,gateway-web/lib/gateway-log-*.ts,book-mall/lib/gateway/**,book-mall/app/api/gateway/logs/**
alwaysApply: false
---

# Gateway 日志 UI 与失败记录规范

完整 UI 说明见 `gateway-web/docs/design.md`。

## 悬停 Tip 尺寸（勿随意改）

- 外框最大高：**680px**（`--gw-log-tip-max-h`）
- 内容区最大高：**580px**（`--gw-log-tip-body-max-h`）
- Params 宽度：**720px**（`--gw-log-tip-width`）
- 真源：`gateway-web/app/globals.css` CSS 变量 + `.gw-log-preview-tip*`（`z-index: 9999`）

## 细滚动条（强制）

- 类名：**`.gw-scrollbar-thin`**
- WebKit 宽 **4px**，滑块半透明白、轨道透明
- 所有 Params / Result / Failed Tip 内容区必须使用，禁止粗滚动条

## Params / Result Tip

- Params：两行 `input:` + JSON、`model:` + 模型名；footer 复制
- 鼠标可移入 Tip；**280ms** 延迟关闭（`use-log-hover-tip.ts`）

## Status 展示

- 圆点颜色用 **inline style**（success `#22c55e` / failed `#ef4444` / running `#f97316`）
- running、pending 带 `#2a2a32` pill 背景
- 实现：`gateway-web/components/logs/log-status-badge.tsx`

## Usage / Token 列（强制语义）

表头：**Usage**（元）与 **Token**（tok）分列（`logs-table.tsx`）。均为 **Gateway 观测**，**不是** Book 钱包扣点、**不是**平台对用户计费。

| 列 | 展示 | 含义 |
|----|------|------|
| **Usage** | 小数 / 两位元 | **挂牌参考费用（元）**（`estimatedVendorCostYuan`，B 表估算，供费用统计） |
| **Usage** | `—` | 无费用估算或任务进行中 |
| **Token** | `4626 tok` 等 | **Token 计量**（悬停可看输入/输出；`metricsSource` 为 VENDOR 或 PLATFORM） |
| **Token** | `—` | 无 token 记录或任务进行中 |

- 实现：`gateway-web/lib/gateway-log-params.ts` · `formatUsageYuanDisplay` · `formatTokenDisplay`
- 厂商 usage 落库规范见 **`.cursor/rules/gateway-vendor-usage-tokens.mdc`**
- 文案/表头变更须同步 `gateway-web/docs/design.md` §2 与本节
- **禁止**在 UI 或文档中把上述列称为「已扣费」「成本结算」「Credits 消耗」

## Failed 悬停（必须）

凡 `status === FAILED`，**必须**能悬停查看原因，禁止「仅有 failed 字样、无 Tip」：

1. **UI**：`LogStatusBadge` 对一切 `FAILED` 启用 `useLogHoverTip`（`enabled: true`），展示 `code` + `message` 两块；锚点加 `title` 作原生兜底、`cursor-help`。
2. **无库内详情时**：`failMessage` 为空则显示固定说明文案（未记录原因 / 建议看 Params），**不得**因 `failMessage`/`failCode` 为空而关闭 Tip。
3. **有详情时**：优先展示库里的 `failCode`、`failMessage`；可复制 `code + message`。

## 服务端写入失败（必须）

`GatewayRequestLog` 标记为 `FAILED` 时，**同时**写入可读的 `failCode` 与 `failMessage`（经 `finalizeRequestLog`）：

| 场景 | failCode 示例 | failMessage |
|------|----------------|-------------|
| 上游 HTTP 非 2xx | `UPSTREAM_HTTP_404` 等 | `summarizeUpstreamFailMessage(...)`，禁止空字符串 |
| 流式 SSE 厂商 error | `STREAM_VENDOR_ERROR` | SSE `error.message` |
| 流中断 / 读流异常 | `STREAM_INTERRUPTED` | 异常 message 或「流式连接中断」 |
| 轮询超时等 | 已有如 `STALE_TIMEOUT` | 已有中文说明 |

- **禁止**：`status: FAILED` 且 `failMessage` 为空（上游空 body 时用 `上游服务返回 HTTP {status}`）。
- **禁止**：用 `GatewayRequiredError` / `GATEWAY_KEY_REQUIRED` 掩盖上游 404 等业务错误（须 `UPSTREAM_ERROR` + 真实 message）。
- Chat 兼容地址：百炼/DashScope 根域名须规范为 `…/compatible-mode/v1`（`resolveOpenAiCompatibleBaseUrl`）。

## 涉及文件

- UI：`log-status-badge.tsx`、`log-preview-tip-shell.tsx`、`logs-table.tsx`
- 写入：`book-mall/lib/gateway/proxy-common.ts`（`finalizeRequestLog`）、`tool-gateway-client.ts` 等
- API：`book-mall/app/api/gateway/logs/route.ts` 须返回 `failCode`、`failMessage`
