---
description: Gateway 厂商凭证 · 平台代付 vs 用户 BYOK（禁止随意写 .env.local）
alwaysApply: true
---

# Gateway 厂商凭证与平台代付

## 调用路径（唯一）

所有子应用（QuickReplica、Canvas、Story、工具站等）调用厂商 AI **必须**：

1. 用户关联 **Gateway API Key**（`sk-gw`）
2. 经 `resolveGatewayAuthForBookUser` → `pickCredentialForKind(providerKind)` 取凭证
3. 经 Gateway 代理层转发厂商 API（写 `GatewayRequestLog`）

**禁止**在业务代码或 `.env.local` 为单个新厂商增加直连 Key 绕过 Gateway。

## 平台代付 vs 用户自付

| 模式 | 凭证存放 | 配置方式 |
|------|----------|----------|
| **平台代付** | Platform Admin Key 绑定的 `GatewayVendorCredential`（channel: `platform-pool`） | **Gateway 模型管理页**（`:3005/dashboard/models`）添加/绑定厂商 Key |
| **用户 BYOK** | 用户 Personal Key 自行绑定的凭证 | 用户在 Gateway 控制台绑定自己的厂商 Key |

新增厂商（如 ElevenLabs）接入时：

- ✅ 登记模型 → Function Models 可见 → **canonical Platform Admin Key** 绑定厂商凭证（Gateway `:3005/dashboard/models`）
- ✅ 业务侧与其它模型相同：`require*Auth` + `pickCredentialForKind`（用户 sk-gw → 平台代付时走托管凭证池）
- ❌ **不要**向 `book-mall/.env.local` / `.env.example` 追加 `VENDOR_API_KEY`
- ❌ **不要**默认把新厂商加入 `platform-credential-seed.ts` 的 env 导入列表

**平台代付 canonical 账号**默认 `13808816802@126.com`（`PLATFORM_POOL_OWNER_EMAIL`）。Book/Gateway 管理员在模型管理页增删凭证时，经 `resolveGatewayCredentialScope` **代管写入 canonical 池**，非写入个人 Gateway 账号。

## Agent 禁止事项

- 未经用户明确要求，**不得**编辑 `.env.local`、`.env.production` 写入厂商 API Key
- 不得建议「把 Key 写进 env 就能用」作为新厂商接入方案
- 厂商 Key 文档（如 `docs/elevenlabs.md`）仅作 API 参考；生产 Key 在 Gateway UI 或部署密钥管理系统配置

## 参考

- Gateway 模型管理：`gateway-web/app/dashboard/models/page.tsx`
- 平台凭证池同步（legacy env bootstrap）：`book-mall/lib/gateway/platform-credential-seed.ts`
- QuickReplica 音频示例：`book-mall/lib/quick-replica/qr-text-to-audio-service.ts` · `requireElevenLabsAuth`
