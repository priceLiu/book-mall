---
description: 禁止 VPN — 本地/容器/生产均不走 VPN 出网或连库
alwaysApply: true
---

# 禁止 VPN（全站强制）

本仓库 **任何场景** 均 **不得** 依赖、要求或引导用户通过 VPN 访问数据库、OSS、Gateway 或厂商 API。

## 禁止

- 在文档、错误提示、Agent 回复、脚本注释中写「请先连 VPN」「需 VPN 连接腾讯云」等
- 为调试/上传/OSS/Prisma 建议「开 VPN 再试」
- 在代码或配置里假设流量经 VPN 代理（如 `HTTP_PROXY` 仅用于 VPN 隧道）
- 把 `DIRECT_DATABASE_URL` 直连内网端口 + VPN 作为 **本地日常开发** 默认路径

## 正确路径

| 目标 | 做法 |
|------|------|
| **PostgreSQL（本地 dev）** | `book-mall/.env.local` 的 **`DATABASE_URL`**：腾讯云 **公网可访问的连接池端口**（见控制台 / `deploy/tencent`），query 带 `connection_limit` / `pool_timeout` / `connect_timeout`；迁移用 `pnpm db:apply-pending`，**不要**依赖 VPN + `DIRECT_DATABASE_URL` + `migrate deploy` |
| **Aliyun OSS** | 本机/服务器 **直连** `*.aliyuncs.com` HTTPS；上传失败查出口网络/防火墙，**不是** VPN |
| **Gateway / 厂商 API** | 直连公网；凭证在 Gateway UI / 部署密钥管理 |
| **book-mall ↔ canvas BFF** | 本地 `localhost` 端口（见 `docs/dev.md`），不经 VPN |

## 数据库不可达时（Agent / 文档统一话术）

1. 检查 `DATABASE_URL` 是否为 **运行时连接池** 地址（非误用 `DIRECT_DATABASE_URL`）
2. 运行 `pnpm --dir book-mall db:ping`
3. 检查腾讯云安全组 / 公网白名单是否放行 **当前出口 IP**
4. 检查 `dev:all` 连接池预算（`docs/dev.md` §数据库连接）；可 `--no-poll` 减压
5. **禁止** 将 VPN 列为解决方案

## Code Review

- PR 新增「VPN」字样（除本 rule 的「禁止 VPN」说明外）→ 改为公网连接池 / 安全组 / 连接数排查
- 用户报告 OSS/DB 慢：先查连接池耗尽、重复上传、BFF 超时；**不得** 仅回复「开 VPN」
