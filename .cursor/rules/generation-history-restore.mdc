---
description: 全站生成/保存必须可恢复继续编辑；素材记录首次来源
alwaysApply: true
---

# 生成历史与工作流恢复（全站）

凡会改图、改视频或产出可下载结果的页面：**每次生成或保存**都必须落历史或工作流快照，用户能重开并继续编辑。禁止结果只在内存、刷新即丢。

## 必须写入

- 结果 OSS URL（图/视频，不写 bytes）
- 工作区 / 节点 / 表单快照（能回到当时状态）
- 模型 `modelKey` 与当时参数
- 所属 `projectId` / `sourceModule`
- **素材首次来源** `firstOrigin`（只写一次，之后只读）

## 首次来源（自动，勿让用户手选）

| `firstOrigin` | 何时 |
|---|---|
| `user-upload` | 本地选文件 |
| `user-paste` | 剪贴板粘贴 |
| `web` | 外链 URL / 拖入网络图 |
| `ecom` | 电商站内资产、本页会话、工具产出 |
| `canvas` | 从画布带入 |
| `story` / `tool` 等 | 按入口补 |

粘贴、本地上传必须自动打标。电商工具箱先接；新上传链路不得省略。

## 恢复

列表/历史点开后须恢复：当前图或视频、左边会话/对照、分层栈或工作流、工具模式。样板：电商「图片处理」`EcomImageLayerProject.workspace`。

## Code Review

- 新生成页没有 persist + 重开恢复 → 驳回
- 只存结果图、没有 workspace/参数 → 驳回
- 上传不写 `firstOrigin` → 驳回
