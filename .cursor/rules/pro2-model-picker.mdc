---
description: 影视专业版 2.0 · 模型选择器统一规范（LLM / 生图 / 生视频）
globs: canvas-web/components/canvas/pro2/**/*
alwaysApply: true
---

# 影视专业版 2.0 · 模型选择器

## 唯一实现（canvas-web）

Pro2 画布及 `canvas-web` 内所有「点击后弹出选择 AI 模型 + 可调参数」的交互：

### 浮动 Dock（强制 · 双钮 Popover）

- **模型**：`LibtvDockEngineModelPicker` 或 role 专用 picker（见 `libtv-generate-settings-spec.md` §0）
- **参数**：`LibtvDockGatewayParamsPicker` 或 `Sbv1ImageDockParamsPicker` / `Sbv1VideoDockParamsPicker` 等
- **锚点 Popover**：`Sbv1ToolbarDropdown` · `useSbv1ToolbarAnchor` · 禁止 Dock 底栏单一合并设置钮

### 全屏 / 嵌套弹层（批量选择等）

- **组件**：`canvas-web/components/canvas/engine-picker.tsx`（`EnginePicker`）
- **弹层**：同文件内 `EngineModelModal`
- **数据**：`useUserProviders()` → Gateway 登记模型；**禁止**前端硬编码模型卡片列表

## 按 role 分流

| 场景 | `EnginePicker` role | 白名单 / 能力 |
|------|---------------------|---------------|
| 故事大纲 / 分镜脚本 LLM | `LLM` | `STORY_LLM_MODEL_KEYS` |
| 三视图 / 分镜图 IMAGE | `IMAGE` | `PRO2_THREE_VIEW_MODEL_KEYS` / `PRO2_FRAME_IMAGE_MODEL_KEYS` 等 |
| 分镜视频 | `VIDEO` | `STORY_PRO_VIDEO_MODEL_KEYS` 等 + `requiredCapabilities` |
| sbv1 视频合成 | `VIDEO` | `SBV1_VOLCENGINE_GATEWAY_MODEL_KEYS` · `gateway:sbv1-volcengine` |

## Gateway role 标签（与 book-mall 登记一致）

统一文案见 `canvas-web/lib/canvas/gateway-model-role.ts` · **`GATEWAY_MODEL_ROLE_META`**：

| Gateway `role` | Dock 分组标题 | 弹层 / 占位 |
|----------------|---------------|-------------|
| `LLM` | Text model · 文本模型 | Select text model |
| `IMAGE` | Image model · 图片模型 | Select image model |
| `VIDEO` | Video model · 视频模型 | Select video model |

`EnginePicker` 触发按钮占位与 `EngineModelModal` 标题 **必须** 走 `gatewayModelRoleMeta(role)`，禁止手写「选择 AI 引擎模型」等旧文案。

## 文本节点 · 类型匹配模型（`story-pro2-starter`）

**唯一入口**：`Pro2TextNodeEnginePickers`（`pro2-text-node-engine-pickers.tsx`）+ `resolvePro2TextNodeEngineRoles`（`pro2-text-node-engine-roles.ts`）。

按节点用途与连线 **只展示相关 Gateway role**，供用户自选模型与参数：

| 用途 / 上下文 | 展示 role | 数据槽 |
|---------------|-----------|--------|
| `pro2TextPurpose=story-outline` | `LLM` | `providerId` / `modelKey` / `params` |
| 预设 `text-to-video` | `VIDEO` | `videoEngine` |
| 预设 `image-to-prompt` | `LLM` + `IMAGE` | `providerId` / … + `imageEngine` |
| 预设 `video-to-prompt` | `LLM` + `VIDEO` | `providerId` / … + `videoEngine` |
| general · 出边连图片节点 | `IMAGE` | `imageEngine` |
| general · 出边连 `sbv1-video-engine` | `VIDEO` | `videoEngine` |
| general · 入边来自媒体（反推） | `LLM` | `providerId` / … |
| general · 无匹配边（独立提示词） | `LLM` + `IMAGE` + `VIDEO` | 三槽均可选 |

用户修改 `IMAGE` / `VIDEO` 时，`syncPro2TextNodeEngineToDownstream` 同步到直连下游 `story-pro2-image` / `sbv1-image` / `sbv1-video-engine` 的 `engine` 字段。

白名单：LLM → `STORY_LLM_MODEL_KEYS`；IMAGE → `SBV1_IMAGE_MODEL_KEYS`；VIDEO（sbv1）→ `SBV1_VOLCENGINE_GATEWAY_MODEL_KEYS`。

## 禁止

- 裸 `<select>` 模型列表 — Dock 用 `LibtvDockEngineModelPicker`；全屏弹层用 `EnginePicker`
- Dock 单一「模型与参数」合并钮 — 须拆为 **模型 + 参数** 双 Popover（见 `libtv-generate-settings-spec.md` §0）
- 复制 `EngineModelModal` 布局另起 Dock Popover — 全屏场景仍走 `EnginePicker`
- 在 Dock / 工具条 / 弹层里写死 `modelKey` 列表而不走 Gateway providers
- 电商工具箱内改走 `EnginePicker`（电商仍用 `StoryboardModelPickerDialog`，见 `ecom-model-picker.mdc`）

## Pro2 已接入入口（须保持）

- `pro2-starter-input-dock` → `Pro2TextNodeEnginePickers`（按 role 分类 · 见上表）
- `pro2-script-input-dock` → `role="LLM"`
- `pro2-three-view-input-dock` · `pro2-frame-generate-picker` · `pro2-character-three-view-picker` → `role="IMAGE"`
- 分镜视频相关弹层 → `role="VIDEO"`

## Code Review

- PR 在 `pro2/**` 出现新的模型选择 UI → **驳回**，改为 `EnginePicker`
- 新增模型时：后端 Gateway 注册 + 前端扩展 `allowedModelKeys` / 默认 pick 函数，不新建选择器组件
