---
description: 电商工具箱全站 UI 基本样式（按钮五层、聊天、表格、图片上传预览、布局）
globs: e-commerce-toolkit/**/*
alwaysApply: false
---

# 电商工具箱 · 设计系统

权威文档：**`e-commerce-toolkit/design/SYSTEM.md`**

编辑 `e-commerce-toolkit` 内 UI 前须对齐母规范；分册：`design/CHAT.md`、`TABLE.md`、`MEDIA.md`、`LAYOUT.md`、`BUTTON.md`、`DIALOG.md`。

## 必守要点

1. **色彩**：仅黑白蓝 + 功能红（`COLORS.md`）；焦点/链接强调 `#0071e3`，主按钮填充 `#0066cc`。
2. **主/次按钮**：`EcomButtonPrimary` / `EcomButtonSecondary` 胶囊（`BUTTON.md`）；Dialog 内主按钮 `rounded-md`（`DIALOG.md`）。
3. **工具条按钮**：卡片/表内用 `rounded-lg` 灰描边（SYSTEM §5.3），**不要**滥用主胶囊。
4. **助手聊天**：气泡样式与 Choice Chip 见 `CHAT.md`；快捷按钮统一 `STORYBOARD_ASSISTANT_CHOICE_CLASS`，禁止深色策划按钮。
5. **表格**：数据表深色表头 `#1d1d1f` + `align-top`（`TABLE.md`）。
6. **图片 / 视频**：展示、预览、上传、资产选取 **必须** 走 `MEDIA.md` §0 / `VIDEO.md` §0 组件映射；禁止自写缩略图或弹层。预览 `EcomImagePreviewDialog`（含缩放）；视频 `EcomVideoPreviewDialog`；上传 `EcomRefUploadCard` + `EcomAssetPickerDialog`；懒加载见 `ecom-media-lazy-load.mdc`。
7. **弹窗**：`useDialogs()`；禁止 `window.alert/confirm`。成功/完成用 **toast**（`DIALOG.md` · `ecom-success-toast.mdc`），错误才 `alert`。
8. **长耗时生成**：≥3min 须移交右下角 `BackgroundGenerationDock`（见 `docs/generation-background-dock.md`）。
9. **生图/生视频模型选择**：唯一弹层 `StoryboardModelPickerDialog`（见 `ecom-model-picker.mdc`），禁止另写选择器。

## Code Review

- 新主 CTA 未走 `EcomButtonPrimary` → 改
- 助手气泡内第二套 choice 栏 → 删
- 表头品牌蓝或单元格未顶对齐 → 改
- 裸 `<video controls>` / 手写 fixed 弹层 → 改
- 图片/视频列表未走 `EcomMediaLibraryTile` → 改
- 参考上传未走 `EcomRefUploadCard` 或「我的资产」未接 `EcomAssetPickerDialog` → 改
- 单图预览未走 `EcomImagePreviewDialog` → 改
- 图片/视频长列表无懒加载 skeleton 或滚动加载态 → 改（见 `ecom-media-lazy-load.mdc`）
