---
description: 漫剧画布固定尺寸、按钮、弹层、文案与对比图设计规范
globs: canvas-web/components/canvas/**,canvas-web/lib/canvas/story-*.ts,canvas-web/lib/canvas/story-pro-node-chrome.ts,canvas-web/lib/canvas/story-edition-chrome.ts,canvas-web/lib/canvas/types.ts,canvas-web/lib/canvas/normalize-graph-nodes.ts,canvas-web/app/globals.css,canvas-web/docs/design.md
alwaysApply: false
---

# 漫剧画布设计规范

完整说明见 `canvas-web/docs/design.md`。**工作流顺序与拆分语义**见 `canvas-web/docs/story-workflow-canonical.md`（开发前必读）。

## 固定尺寸（勿随意改常量）

- 故事主题 + 故事大纲：**1020×1200**（`STORY_CONTROL_NODE_*` / `STORY_PRO_CONTROL_NODE_*`）
- 角色列（三视图）宽 **560**；分镜列宽 **1080**；分镜视频列宽 **500**；高度按行数估算（`story-column-layout.ts`，上限 2100）
- 标准流程见 **story-workflow-canonical.md**：定稿 → 输出工作流（仅拆分）→ 用户手动生成媒体
- 多工作流须按 `scriptHubId` / `hubNodeId` 隔离
- 三视图：**670×880**
- 行内参考图 **220**、输出图 **248**
- 真源：`story-node-chrome.ts`、`story-pro-node-chrome.ts`、`types.ts` `NODE_DEFAULT_SIZE`、`story-ref-image.ts`

控制节点固定尺寸；角色/分镜/视频列高度 = **头部 + 单行块高 × 行数 + 100px**，最小 **2100px**。分镜行块高约 **320px**；视频槽 **280px** 高、列宽 **540px**。

## 文案色阶（提示 / 状态 / 错误）

- **提示**（@ 说明、前置条件、锁定占位）：`STORY_HINT_*` 金黄 · 见 `design.md` §8
- **节点 chrome**（导航、标题、状态）：`STORY_CHROME_GREEN_CLASS` / `PRO_HINT_LABEL_CLASS` 绿色
- **错误**：`StoryErrorLine` · 单行 `truncate` + `title` 悬停全文
- 规则详情：`.cursor/rules/canvas-story-typography.mdc`

## 按钮

- **快手漫剧**主操作：`STORY_ORANGE_BTN_CLASS` / `STORY_NODE_ACTION_BTN_CLASS`，**h-9**
- **影视专业版**主操作：`PRO_NODE_ACTION_BTN_CLASS` · 青描边（`story-pro-node-chrome.ts`）
- 列批量：`storyEditionBatchBtnClass(edition)`（`story-edition-chrome.ts`）
- 底栏骨架：`StoryNodeFooterShell`（按钮 + 16px 提示占位）
- 弹层 Tab/保存：快手橙 · 专业版青 — `storyEditionModalTabClass` / `Save` / `Outline`（§14）

## 影视专业版（Story Pro）

- 真源：`story-pro-node-chrome.ts` + `story-edition-chrome.ts` · 全文 `design.md` **§14**
- 节点 `story-pro-*` · 共用列传 `edition="pro"`
- 审阅弹层：`StoryScriptHubModal edition="pro"`；导演提示词：`proDirectorPack`
- 入库：`StoryProAssetImportIcon`（禁止「入库」二字）
- **禁止**专业版 UI 使用橙色 `#fb923c` 作主强调

## 弹出层

- 审阅 / Word / 对比：`z-[1100]`，`createPortal`，`body overflow hidden`，Esc 关闭
- 双栏：`grid-cols-2` 白卡片，左编辑右 `MarkdownView document`，左右等高（ResizeObserver）
- **模板 Tab 只在故事主题弹层顶栏**，不在节点外壳上
- 确认/提示：`useDialogs()`，禁止 `window.alert/confirm/prompt`

## 文案显示

- 故事主题节点：**黑底** `darkPreview`；故事大纲：**白纸** `nodePreview`
- 角色/分镜默认可表格编辑，可切 Markdown 源码；**故事大纲**默认 `StoryOutlineDocumentEditor` 块级渲染编辑（见 `design.md` §6.4）
- Word 全屏：`MarkdownFullscreenLightbox`，820px 纸宽
- **文案编辑 UX 铁律**见 `design.md` §6.4（致命：左栏须渲染/表格态，禁止默认整篇源码与未验证 WYSIWYG）

## 参考图 / 对比

- `@` 激活参考图：快手橙 / 专业版青 — `storyEditionActiveRefBorderClass(edition)`
- 分镜 @ 宫格：3 列、`content-start`、悬停 **Eye**、点击全屏预览（§7.1）
- 引擎对比：`CompareModal` 滑块重叠对比，非简单并排

## 生成中动效与预览图标（§15）

- **扫光 CSS**：`globals.css` · `.canvas-story-media-generating` / `-pro` · `storyEditionGeneratingBorderClass(edition)`
- **左 @ 参考图列**（220×248）：`upstreamGenerating={frameRunning}` · **仅整列外框扫光** · **禁止** `RefreshCw` · 禁止宫格 `min-h-full`
- **右输出图列**（248）：扫光 + 中央 **`RefreshCw` + `storyEditionSpinClass`**
- **悬停预览**：列内宫格/槽位用 **Eye**；节点头审阅用 `StoryPreviewMagnifyButton`（Search）
- **入库**：`StoryProAssetImportIcon`（Layers）· 生视频 **Clapperboard** · 过审 **Check**（无驳回）

## 视频播放

- 唯一组件：`CanvasVideoPlayer`（`design.md` §12）；禁止节点/弹层第二套控制条

## 图片上传

- **必须**同时支持：点击、拖入、粘贴（`design.md` §13）
- 真源：`lib/canvas/image-upload-handlers.ts` · `MediaHoverBox.onImageFile`
- 多槽须激活目标后再 paste；drop/paste 须 `stopPropagation` 避免画布全局建节点

## 改尺寸时

同步 `normalize-graph-nodes.ts` 与 `docs/design.md` §1。
