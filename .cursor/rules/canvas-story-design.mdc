---
description: 漫剧画布固定尺寸、按钮、弹层、文案与对比图设计规范
globs: canvas-web/components/canvas/**,canvas-web/lib/canvas/story-*.ts,canvas-web/lib/canvas/types.ts,canvas-web/lib/canvas/normalize-graph-nodes.ts,canvas-web/docs/design.md
alwaysApply: false
---

# 漫剧画布设计规范

完整说明见 `canvas-web/docs/design.md`。**工作流顺序与拆分语义**见 `canvas-web/docs/story-workflow-canonical.md`（开发前必读）。

## 固定尺寸（勿随意改常量）

- 故事主题 + 故事大纲：**1020×1200**（`STORY_CONTROL_NODE_*`）
- 角色列（三视图）：**560×2100**；分镜列：**1080×2100**；分镜视频列：**500×2100**；剪映：**400×280**（顶对齐控制行）
- 标准流程见 **story-workflow-canonical.md**：定稿 → 输出工作流（仅拆分）→ 用户手动生成媒体
- 多工作流须按 `scriptHubId` / `hubNodeId` 隔离
- 三视图：**670×880**
- 行内参考图 **220**、输出图 **248**
- 真源：`story-node-chrome.ts`、`types.ts` `NODE_DEFAULT_SIZE`、`story-ref-image.ts`

控制节点与角色/分镜列：**外框固定尺寸**，内容区滚动。禁止再按 rows 动态撑节点高。

## 蓝色提示

- 模型标签 / @ 说明：`STORY_HINT_LABEL_CLASS`、`STORY_HINT_BODY_CLASS`（`#60a5fa` + 左竖线）
- 真源：`story-column-sync.ts` · 详见 `design.md` §8

## 按钮

- 漫剧主操作：`STORY_ORANGE_BTN_CLASS` / `STORY_NODE_ACTION_BTN_CLASS`，**h-9**，禁用不变暗
- 底栏骨架：`StoryNodeFooterShell`（按钮 + 16px 提示占位）
- 弹层 Tab 选中：`bg-[#fb923c]/25 text-[#fdba74]`；保存实心橙

## 弹出层

- 审阅 / Word / 对比：`z-[1100]`，`createPortal`，`body overflow hidden`，Esc 关闭
- 双栏：`grid-cols-2` 白卡片，左编辑右 `MarkdownView document`，左右等高（ResizeObserver）
- **模板 Tab 只在故事主题弹层顶栏**，不在节点外壳上

## 文案显示

- 故事主题节点：**黑底** `darkPreview`；故事大纲：**白纸** `nodePreview`
- 角色/分镜默认可表格编辑，可切 Markdown 源码；**故事大纲**默认 `StoryOutlineDocumentEditor` 块级渲染编辑（见 `design.md` §6.4）
- Word 全屏：`MarkdownFullscreenLightbox`，820px 纸宽
- **文案编辑 UX 铁律**见 `design.md` §6.4（致命：左栏须渲染/表格态，禁止默认整篇源码与未验证 WYSIWYG）

## 参考图 / 对比

- `@` 激活参考图：橙框 `#fb923c` + glow；未激活 `border-white/15`
- 引擎对比：`CompareModal` 滑块重叠对比，非简单并排

## 改尺寸时

同步 `normalize-graph-nodes.ts` 与 `docs/design.md` §1。
