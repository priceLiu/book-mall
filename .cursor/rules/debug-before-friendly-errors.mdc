---
description: 排障优先于改错误提示；禁止用友好文案掩盖未定位的根因
alwaysApply: true
---

# 排障优先 · 禁止「只改提示」

用户报告功能失败（生成失败、500、参数缺失等）时：

## 必须先做

1. **复现链路**：从 UI → 前端入队 → API → Gateway → 厂商，逐步核对请求体是否含预期字段。
2. **查日志/任务表**：`CanvasGenerationTask.failMessage`、`GatewayRequestLog`、服务端终端输出。
3. **定位根因**：路由错误、字段名不一致、分支顺序、空 body、凭证/环境问题等。
4. **修根因**：改路由、传参、校验顺序或业务逻辑；必要时补回归测试。

## 禁止

- 在**未确认根因**时，仅修改 `friendly-task-error`、toast 标题、节点角标文案等「友好提示」当作修复。
- 用笼统文案（如「请检查 prompt/参考图」）替代已查明但尚未修复的代码问题。
- 未经用户确认，擅自扩大改动范围（无关重构、额外 UX）。

## 不清楚时

- 向用户说明已查到的证据与缺口（例如：前端有参但 createTask body 为空）。
- 列出 1～2 个最可能原因，请用户协助（是否重启服务、贴 Gateway 日志、操作步骤）。
- **不要**自行假设「用户没填 prompt」而只改提示。

## 例外

- 根因已修复 **之后**，可顺带改进错误分类/文案，且须在 PR/说明中写清「根因 + 提示变更」。
