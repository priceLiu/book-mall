import { ImageLayerStudio } from "@/components/image-layer/image-layer-studio";

export const metadata = {
  title: "图片处理",
};

export default function ImageLayerPage() {
  return <ImageLayerStudio />;
}
