# book-mall 主站地址
BOOK_MALL_ORIGIN=http://localhost:3000

# 本站点对外地址（SSO 回调等）
GATEWAY_PUBLIC_ORIGIN=http://localhost:3005

# 与 book-mall 相同的 Gateway SSO 服务端密钥（≥16 字符，必须与 book-mall 一致）
GATEWAY_SSO_SERVER_SECRET=bm-local-tools-sso-srv-2026-change-for-production-key-min16
